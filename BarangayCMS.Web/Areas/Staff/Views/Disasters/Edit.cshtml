@model BarangayCMS.Web.Areas.Staff.Models.DisasterViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "~/Areas/Staff/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "I-edit ang Insidente";
}

<div class="container mt-4" style="max-width: 650px;">
    <div class="card shadow">
        <div class="card-header bg-warning text-dark">
            <h4 class="mb-0"><i class="bi bi-pencil-square"></i> Baguhin ang Detalye ng Insidente</h4>
        </div>
        <div class="card-body">
            <form asp-area="Staff" asp-controller="Disaster" asp-action="Edit" asp-route-id="@Model.Id" method="post">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="Id" />

                <div class="mb-3">
                    <label asp-for="DisasterType" class="form-label fw-bold"></label>
                    <input asp-for="DisasterType" class="form-control" readonly />
                </div>

                <div class="mb-3">
                    <label asp-for="Location" class="form-label fw-bold"></label>
                    <input asp-for="Location" class="form-control" />
                    <span asp-validation-for="Location" class="text-danger"></span>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="DateOccurred" class="form-label fw-bold"></label>
                        <input asp-for="DateOccurred" class="form-control" type="datetime-local" />
                        <span asp-validation-for="DateOccurred" class="text-danger"></span>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label asp-for="Status" class="form-label fw-bold"></label>
                        <select asp-for="Status" class="form-select">
                            <option value="Active">Active</option>
                            <option value="Controlled">Controlled</option>
                            <option value="Resolved">Resolved</option>
                            <option value="Cleared">Cleared</option>
                        </select>
                        <span asp-validation-for="Status" class="text-danger"></span>
                    </div>
                </div>

                <div class="mb-3">
                    <label asp-for="Description" class="form-label fw-bold"></label>
                    <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>

                <div class="mt-4 d-flex justify-content-between">
                    <a asp-area="Staff" asp-controller="Disaster" asp-action="Index" class="btn btn-secondary">I-cancel</a>
                    <button type="submit" class="btn btn-warning px-4 fw-bold">I-save ang Pagbabago</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}